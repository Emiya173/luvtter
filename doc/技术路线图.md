# 信件应用 技术路线图

**生成时间**: 2026-04-22（末次更新同日）
**对照文档**: `信件应用-功能设计文档.md`、`信件应用-技术设计文档.md`

记录功能清单与当前实现状态。状态标记：
- ✅ 已实现并联通端到端
- 🟡 部分实现（仅服务端 / 仅客户端 / 仅骨架）
- ⬜ 未开始

---

## 0. 基础设施（Stage 1）

| 项 | 状态 | 备注 |
|----|------|------|
| Gradle 多模块（api-contract / shared / composeApp / server） | ✅ | KMP + JDK 17 + Kotlin 2.3.20 |
| Compose Multiplatform 脚手架（Desktop/Android/iOS target） | ✅ | Desktop 主验证，Android/iOS 框架就位 |
| Ktor 服务端 + Netty + ContentNegotiation + StatusPages + CallLogging + CORS | ✅ | |
| Postgres + Flyway 迁移 + HikariCP | ✅ | V1/V2/V3 已落库 |
| Koin DI（服务端） | ✅ | `koin-ktor` plugin 接入，service 按域分模块（auth/user/stamp/mail），路由 `get()` 注入 |
| Koin DI（客户端） | ✅ | composeApp 已迁移到 Koin 4.1（`koin-compose-viewmodel` + `koin-compose-viewmodel-navigation`），各 Api/VM 单 single 注册 |
| MinIO 对象存储 + 预签名上传/下载 | ✅ | `StorageService` 启动时自建 bucket；`POST /api/v1/uploads/photo/{sign-put,sign-get}` 颁发短期 presigned URL；附件以 `object_key` 持久化，`list/detail` 时即时再签发；`MediaUploadFlowTest` 端到端验证（testcontainers-minio 真上传 + 字节回读 + 越权拒绝） |
| Argon2 密码哈希 + JWT(HS256) 认证 | ✅ | |
| CI / 测试容器（testcontainers-postgres + minio） | ✅ | `server:test` 覆盖 auth/send/attachment/segment/sse/sessions/media-upload/sse-heartbeat-signals 八条 happy path（14 个用例） |
| API 契约共享 DTO（kotlinx.serialization） | ✅ | Auth/Address/Contact/Catalog/Letter/Notification/Folder |
| **日志**（logback RollingFileAppender + kotlin-logging 多平台版） | ✅ | 服务端 `logs/server.log` 按天滚动；关键事件已打点 |
| **客户端日志** | ✅ | `kotlin-logging` 通用模块在 shared API 模块中打点 |

---

## 1. 账号系统

| 项 | 状态 | 备注 |
|----|------|------|
| 邮箱注册 / 登录 / 刷新 / 登出 | ✅ | 服务端 + 桌面 UI 登录注册页 |
| Apple / Google OAuth 登录 | ⬜ | |
| 两步式 handle（注册临时 → 首次设置最终） | ✅ | 客户端 Home 顶栏提示「临时」并提供设置专属 handle 的对话框 |
| Session 列表 / 设备管理 | ✅ | `GET/DELETE /api/v1/me/sessions` + 客户端 `SessionsScreen`（顶栏"设"入口），撤销后对应 refresh token 立即失效 |
| "给未来的自己寄第一封信" 引导 | ⬜ | |

---

## 2. 地址系统

| 项 | 状态 | 备注 |
|----|------|------|
| 多地址 CRUD | ✅ | 服务端 + Desktop AddressesScreen |
| 默认地址 | ✅ | |
| 真实地址（经纬度） | ✅ | |
| 虚拟地址（预设锚点 / 自定义锚点 + 相对距离） | ✅ (服务端) / 🟡 (客户端) | 桌面端已支持预设锚点，自定义锚点未做 |
| 虚拟锚点池 seed | ✅ | V3 已 seed 4 个锚点 |
| 软删除（不影响在途信件距离） | ✅ | 距离已 snapshot 在 letters 表 |

---

## 3. 联系人 / 隐私

| 项 | 状态 | 备注 |
|----|------|------|
| 联系人 CRUD（备注、关系、标签） | ✅ | 桌面端 ContactsScreen：handle 查找 → 加为联系人 → 列表/删除 |
| handle 查询 + acceptsFromMe | ✅ | 桌面端在 ContactsScreen 与 ComposeScreen 写信前调 lookup |
| 屏蔽 / 解除屏蔽 | ✅ | 桌面端 ContactsScreen 查询结果可"屏蔽"；底部"已屏蔽用户"列表含"取消屏蔽"按钮 |
| only_friends 隐私开关 | ✅ | ContactsScreen 顶部 Switch 即时调 `PATCH /api/v1/me` |

---

## 4. 写信体验

| 项 | 状态 | 备注 |
|----|------|------|
| 草稿 CRUD + 列表 + 详情 | ✅ | Desktop "草稿" Tab：列表 / 点击进入编辑 / "删除草稿"按钮；ComposeScreen 支持 `editDraftId` 编辑模式与"保存草稿"按钮 |
| 键盘文本输入（segments 结构） | ✅ | 客户端单段，未支持多段/划线 |
| 涂改机制（划线、几秒删除窗口、主动划线） | ⬜ | 数据结构已就绪 |
| 草稿封存（冷静期） | ✅ | ComposeScreen "封存草稿" 按钮 → 分钟预设 / 自定义分钟 → ISO8601 写入 `sealedUntil` |
| 信纸样式选择 | ✅ | ComposeScreen 拉取 `/api/v1/stationeries` 渲染 chip |
| 字体选择（按封） | ✅ | ComposeScreen 4 档内置选项（默认/楷/宋/手写） |
| 手写输入（笔画 JSON） | ⬜ | |
| 语音输入 | ⬜ | |
| 扫描输入（保留原貌 / 重绘到信纸） | ⬜ | 需 image-worker |

---

## 5. 寄信与送达

| 项 | 状态 | 备注 |
|----|------|------|
| 邮票四档 + 容量 + 速度系数 | ✅ | seed 完成 |
| 重量校验（附件 ≤ 邮票容量） | ✅ (服务端) | 当前文本信权重为 0 |
| 距离算法（haversine / 虚拟距离） | ✅ | `mail/Distance.kt` |
| 送达时间分段函数 + ±10% 抖动 + 1h~14d 钳制 | ✅ | `mail/Delivery.kt` |
| 寄出流水（校验→扣库存→落地→生成事件） | ✅ | `LetterService.send` |
| 选择寄到对方哪个地址（按 §3.3） | ✅ | 寄信前 `GET /api/v1/users/by-handle/{handle}/addresses` 仅暴露 label/type，不泄露经纬度 |
| 测试加速：将在途信件压缩到 N 秒后送达 | ✅ | `POST /api/v1/letters/{id}/expedite?seconds=5`（仅寄件人，上限 1h）；Outbox 行内"加速到达"按钮 |
| 拒收（onlyFriends / blocks） | ✅ | |
| 寄件人不可见距离，仅看 transitStage | ✅ | |
| 不可撤回（无删除/修改 API） | ✅ | |

---

## 6. 收件 / 阅读

| 项 | 状态 | 备注 |
|----|------|------|
| 收件箱（自动 in_transit→delivered 升级） | ✅ | Desktop Inbox 标签；按"当前位置"过滤，详见下条 |
| 当前位置机制（用户在 A 时只看寄到 A 的信，寄到 B 的信只触发通知） | ✅ | `users.current_address_id`；`POST /api/v1/me/current-address` 切换；通知带 `addressId/addressLabel` |
| 寄件箱 + transitStage 三档 | ✅ | Desktop Outbox 标签 |
| 信件详情（含 segments 渲染、划线显示） | ✅ | Desktop 已支持 LineThrough |
| 标记已读（幂等） | ✅ | Desktop 进入详情自动调用 |
| 隐藏（双向软隐藏） | ✅ | Desktop 行内"隐藏"按钮；顶部"查看已隐藏"切换可见已隐藏视图，"恢复"按钮取消隐藏（永不删信件） |
| 收/发件箱显示收件地址 | ✅ | LetterRow 渲染 `recipientAddressLabel`，发件方"→ 寄到「家」"，收件方"→ 收件地址：家" |
| 拆信封动画（首次） | ⬜ | |
| 全屏 / 缩放 / 翻页阅读 | ⬜ | |

---

## 7. 附件与重量

| 项 | 状态 | 备注 |
|----|------|------|
| 附件表 + 类型字段 | ✅ (表) | |
| 添加照片附件 API | ✅ | `POST /api/v1/letters/drafts/{id}/attachments`，支持 `mediaUrl` 直传或 `objectKey` 引用 MinIO 对象（每次读取重签发 GET URL） |
| 添加贴纸 API | ✅ | `POST /api/v1/letters/drafts/{id}/stickers` |
| 删除附件 + totalWeight 回滚 | ✅ | |
| 邮票承重校验（超重报错提示升级） | ✅ | `WEIGHT_EXCEEDED` |
| 客户端附件 UI | ✅ | Desktop ComposeScreen 已支持贴纸选择器 + **MinIO 直传选图按钮**(`MediaApi.uploadPhoto` 走 `sign-put → PUT → done`,客户端再 `addPhoto(objectKey)`);重量按 `⌈sizeKB/10⌉` 估算 |
| 客户端 MinIO 上传链路 | ✅ | `shared/network/MediaApi`(signPut/signGet/notifyDone/uploadPhoto) + `composeApp/platform/FilePicker`(expect/actual,Desktop=JFileChooser,Android/iOS stub);`createRawHttpClient()` 用于直传到 S3,不带 JWT/JSON 默认头 |

---

## 8. 拟真事件

| 项 | 状态 | 备注 |
|----|------|------|
| 寄信时一次生成事件（提前 / 磨损 / 明信片） | ✅ | `mail/Events.kt`，2% / D>500 & >72h / 48-120-240h |
| 事件读 API（详情已带，独立列表） | ✅ | `GET /api/v1/letters/{id}/events` |
| 客户端事件展示（途中明信片浮层） | 🟡 | 详情页"途中事件"列表渲染；浮层动画未做 |

---

## 9. 整理（分类 / 收藏 / 搜索）

| 项 | 状态 | 备注 |
|----|------|------|
| 分类 folders CRUD + reorder | ✅ | `/api/v1/folders`；Desktop "分类" Tab 内嵌 chips，可新建/删除分类，点击 chip 查看分类下信件 |
| 信件→分类 | ✅ | `POST /api/v1/letters/{id}/folder`；详情页 "移到分类" 对话框；新增 `GET /api/v1/folders/{id}/letters` 列表 |
| 收藏 | ✅ | `POST/DELETE /api/v1/letters/{id}/favorite`；详情页"收藏/取消收藏"按钮；新增 `GET /api/v1/me/favorites` 列表 + Desktop "收藏" Tab；LetterRow 标题旁 ★ 标记 |
| 全文搜索 tsvector + GIN | ✅ | V6 安装 `letter_bigram(text)` + `letter_bigram_query(text)` plpgsql 函数 + `letter_contents` BEFORE INSERT/UPDATE 触发器自动维护 `index_tsv`；`LetterService.search` 改为 `index_tsv @@ letter_bigram_query(?)`；Desktop Home 顶栏"搜"按钮弹出搜索对话框 |
| 中文 bigram | ✅ | `letter_bigram` 对 CJK 同时输出单字 + 相邻 bigram，ASCII 归一为小写词；`SearchFlowTest` 验证「海/回家/HEART/海风」六组查询 |
| 回信关联 thread | ✅ | 详情页"回信"按钮预填收件人 + `replyToLetterId`；详情页渲染"↩ 回复此前的来信"标识 |

---

## 10. 通知

| 项 | 状态 | 备注 |
|----|------|------|
| notifications 表 + 偏好表 | ✅ | |
| 列表 / 标记已读 / 标记全部已读 / 未读计数 | ✅ | `/api/v1/notifications/*` |
| 偏好读写 API | ✅ | `/api/v1/notifications/prefs`；新增 `quietStart/quietEnd/timezone` 三字段（V7 迁移）|
| 免打扰时段（Quiet Hours） | ✅ | 半开区间 `[start,end)`，跨日(start>end)语义，按 IANA 时区计算本地小时；静默期内通知仍落库但不再走 SSE 推送；非法 timezone 422；`NotificationQuietHoursTest` 4 用例（覆盖 / 非覆盖 / 非法 tz / 清空）|
| 信件送达时自动触发 new_letter 通知 | ✅ | `NotificationService.emit` 在 inbox 自动投递时 |
| 客户端未读 Badge（Desktop Home） | ✅ | 顶栏铃铛按钮 |
| SSE 长连接 | ✅ | `/api/v1/notifications/stream`，客户端 `NotificationApi.stream()` 订阅，HomeViewModel 实时前置 |
| SSE 心跳（ping） | ✅ | 服务端按 `notifications.heartbeatSeconds`（默认 25）发 `event=ping`；ready 事件回传当前心跳间隔；测试以 1s 周期验证 |
| SSE 瞬时信号通道（不入库） | ✅ | `event=signal` + `SignalDto`，与持久 `notification` 双轨。已落地 `upload_done`（`POST /api/v1/uploads/photo/done` 触发,通知本人其他在线设备）+ `letter_read`（收件人调 `markRead` 时通知寄件人）|
| 应用图标 Badge | ⬜ | |

---

## 11. 邮票 / 信纸 / 贴纸资源

| 项 | 状态 | 备注 |
|----|------|------|
| 目录 API（stamps / stationeries / stickers） | ✅ | |
| 我的资源 `/me/assets` | ✅ | |
| 注册赠送（50 平信 + 各 5） | ✅ | |
| 每日奖励 API（按用户时区幂等） | ✅ | `POST /api/v1/me/daily-reward`，客户端启动自动领 |
| 邮票限定款 / 商店 | ⬜ | V2 |

---

## 12. 异步管线 / 上传

| 项 | 状态 | 备注 |
|----|------|------|
| async_tasks 表 + pending 部分索引 | ✅ | |
| `/uploads/scan` / `/uploads/handwriting` | ⬜ | |
| Python image-worker（透视矫正 / 笔迹提取） | ⬜ | |
| PaddleOCR 索引 | ⬜ | |

---

## 13. 客户端

| 平台 | 状态 | 备注 |
|------|------|------|
| Desktop（Compose JVM） | 🟡 | 登录 / 注册 / 收发件箱（按当前位置过滤）/ 写信（含收件人地址选择）/ 详情 / 地址 / 联系人 / 当前位置切换 / handle finalize / 通知列表（含切换地址按钮）/ 每日奖励；按屏幕分辨率自适应缩放（4K → 2.0x）；架构：AndroidX Lifecycle ViewModel KMP + Navigation Compose 类型安全路由 + Koin DI + Screen/Content 分离 + 状态全部上提至 stateful 包装层 |
| Android | ⬜ | scaffold 就绪，未编写 UI 适配 |
| iOS | ⬜ | scaffold 就绪 |
| Web (Wasm) | ⬜ | |
| 本地 SQLDelight（草稿、缓存） | ⬜ | |
| 自然语言时间 / 文学化文案 / 信封飞过 loading | ⬜ | 气质打磨阶段 |

---

## 路线图建议（基于当前进度）

按技术文档 §6.1 的阶段对应：

- **Stage 1（基础设施）**：✅ 完成
- **Stage 2（核心流程：账号 / 地址 / 联系人 / 单档邮票 / 文本信寄收读）**：✅ 服务端完成；客户端 Desktop 已可端到端验证；缺联系人/handle 引导 UI
- **Stage 3（写信体验：手写、涂改、字体、信纸、拆信动画）**：⬜
- **Stage 4（扫描 + OCR）**：⬜（需引入 image-worker）
- **Stage 5（多地址、虚拟锚点、邮票多档、每日奖励、附件、拟真事件、回信、通知）**：✅ 服务端基本就绪（多地址/虚拟锚点/邮票多档/事件/每日奖励/附件/通知/分类/收藏）；缺回信线索专用 API、搜索 API、通知 SSE、客户端附件/分类 UI
- **Stage 6（打磨发布）**：⬜

### 短期（建议下一两轮迭代）

1. 客户端：~~联系人界面 + handle 引导 + 草稿管理~~（✅ 已完成）+ 附件/贴纸 UI + 分类/收藏 UI（✅ 已完成）+ 涂改的多 segment 编辑
2. 服务端：~~search API~~（✅ V6 起改用 tsvector + 中文 bigram + GIN）、~~回信 thread API~~（✅）、~~letter events 独立 API~~（✅）、~~notifications SSE 长连接~~（✅）、~~tsvector + bigram 搜索升级~~（✅）
3. 工程：~~客户端 Koin DI~~、~~服务端 Koin 模块化重构~~、~~testcontainers 集成测试 1~2 个关键路径（auth、send→inbox）~~（✅ 已完成）

### 当前最小阻塞清单（按优先级）

- ~~**A. 服务端 Koin 模块化**~~：✅ 已完成。`di/Modules.kt` 拆分为 `configModule/authModule/userModule/stampModule/mailModule`，路由通过 `get()` 注入
- ~~**B. testcontainers 集成测试**~~：✅ 已完成。`server/src/test/.../AuthFlowTest` + `SendFlowTest` + `AttachmentFlowTest` 覆盖 register→login→/me、draft→send→expedite→inbox→markRead、以及 draft→sticker+photo→detail→delete 三条 happy path；`PostgresContainer` 单次启动 + 迁移，测试间 `TRUNCATE` 隔离
- ~~**C. 写信附件 UI**~~：✅ 已完成。`LetterApi` 新增 list/addPhoto/addSticker/delete；`ComposeViewModel` 暴露 `addStickerAttachment / pickAndUploadPhoto / removeAttachment`；`ComposeScreen` 新增附件列表 + 重量显示 + 贴纸选择器 + **选图按钮直接走 MinIO 直传链路**（`MediaApi` + 平台 `FilePicker` expect/actual,Desktop JFileChooser）
- ~~**D. 多段 segment 编辑**~~：✅ 已完成。`ComposeUiState.segments: List<TextSegment>` 替代单段 `content`；VM 暴露 `onSegmentTextChange / onSegmentStyleToggle / onSegmentAddAfter / onSegmentRemove`；`ComposeScreen` 以 `SegmentEditorRow` 列表呈现,每段独立 TextField + 划掉切换（`TextDecoration.LineThrough`）+ 下方新增 + 删除；`SegmentRoundTripTest` 验证多段含 strikethrough 完整往返
- ~~**E. 通知 SSE**~~：✅ 已完成。服务端 `install(SSE)` + `sse("/api/v1/notifications/stream")`（auth-jwt），`NotificationService.emit` 经 per-user `MutableSharedFlow` 推送 `ServerSentEvent(event="notification")`；客户端 shared `HttpClient` 装 `SSE` 插件，`NotificationApi.stream(): Flow<NotificationDto>` 通过 `sseSession.incoming` 解码；`HomeViewModel` 订阅流,新事件前置到列表并自增未读；`NotificationStreamTest` 端到端验证收件人侧 new_letter 事件送达

### 中期

4. Stage 3 写信体验：手写笔画数据结构 + 字体下载 + 信纸渲染
5. image-worker（Python + OpenCV + PaddleOCR）容器与 `/uploads/scan` 接口
6. ~~客户端接入 MinIO 上传链路：选图 → `sign-put` → `PUT` → `addPhoto(objectKey)`~~（✅ Desktop 已贯通；Android/iOS FilePicker actual 待平台 UI 起步时补）

### 长期

6. Android / iOS 适配与 SQLDelight 本地化
7. Stage 6 打磨：自然语言时间、信封 loading、拆信动画、Badge
